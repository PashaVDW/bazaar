<div class="bg-white py-10 px-6 rounded-lg shadow-md">
    <h2 class="text-2xl font-bold text-gray-800 mb-6 text-center">{{ __('messages.our_values') }}</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-center">
        <div>
            <div class="text-orange-500 text-4xl mb-2">⭐</div>
            <h3 class="font-semibold text-lg">{{ __('messages.quality') }}</h3>
            <p class="text-sm text-gray-600">{{ __('messages.quality_description') }}</p>
        </div>
        <div>
            <div class="text-orange-500 text-4xl mb-2">⚡</div>
            <h3 class="font-semibold text-lg">{{ __('messages.speed') }}</h3>
            <p class="text-sm text-gray-600">{{ __('messages.speed_description') }}</p>
        </div>
        <div>
            <div class="text-orange-500 text-4xl mb-2">❤️</div>
            <h3 class="font-semibold text-lg">{{ __('messages.care') }}</h3>
            <p class="text-sm text-gray-600">{{ __('messages.care_description') }}</p>
        </div>
    </div>
</div>
